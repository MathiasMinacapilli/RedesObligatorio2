// proxy.cc

void proxy() {
    //Examen diciembre 2017
    /*
    int serverSock = new socket();
    bind(serverSock, localhost, 80);
    while (true) {
        int clientSock = accept(serverSock); //espero conexiones
        if (clientSock < 0)
            break;
            thread.new (atenderCliente, clientSock); //nuevo hilo para el cliente
    }
    close (serverSock);
    */
}
